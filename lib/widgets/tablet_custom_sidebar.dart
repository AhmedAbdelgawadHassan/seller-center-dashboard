import 'package:flutter/material.dart';

class TabletCustomSidebar extends StatelessWidget {
  const TabletCustomSidebar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}